language: java
os: linux
dist: xenial
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
jdk:
  - openjdk8
branches:
  only:
    - master
    - stable
install: true
before-_cript: gradle check
script: gradle build
